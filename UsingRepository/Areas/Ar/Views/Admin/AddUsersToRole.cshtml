@using System.Collections.Generic;
@using UsingRepository.Areas.ViewModels;
@model List<UserRoleViewModel>

@{
    ViewBag.Title = "Manage Roles";
    var roleId = ViewBag.RoleId;
}

<h2 class="alert alert-info">ادارة اليوزرز الموجودين فى صلاحية معينة</h2>

@using (Html.BeginForm())
{
   @Html.Hidden("roleId")
    <div class="row col-md-8">
        <div class="panel panel-primary" style="overflow: hidden">
            <div class="panel-heading">
               إضافة أو حذف مستخدمين لهذه الصلاحية
            </div>
            <div class="panel-body">

                @for (int i = 0; i < Model.Count; i++)
                {
                    @Html.HiddenFor(m => Model[i].UserId)
                    @Html.HiddenFor(m => Model[i].Email)
                    <div class="checkbox">
                        <label>
                            @Html.CheckBoxFor(ur => Model[i].IsSelected) @Model[i].Email
                        </label>
                    </div>
                }
            </div>
            <div class="panel-footer">
                <button type="submit" class="btn btn-Black">تعديل</button>
                @Html.ActionLink("الغاء", "RoleForm", new { roleId = roleId }, new { @class = "btn btn-primary" })
            </div>
        </div>
    </div>
}
